import { createComponent } from '@iwsdk/core';

// Authored on invisible solid prototypes; activated after hierarchy attachment.
export const StaticCollision = createComponent('StaticCollision', {});
